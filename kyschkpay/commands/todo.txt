1. Bug เมื่อเพิ่ม proxy เข้าไปแล้วระบบทำงานไม่ถูกต้อง
2. ที่หน้างาน tab เว็บ site กยศ ถ้าระบบ check pay มีปัญหา ระบบจะให้ user login แบบ manual ต้องแก้ flow ส่วนนี้คือเปลี่ยนเป็นให้ไปที่ระบบ check pay เพื่อให้ reload ให้ใหม่
3. ที่หน้างาน tab เว็บ site จะมีปัญหากับ proxy authen ด้วย
3. DMS ตอน update date ข้อมูลหน้างาน จะมีปัญหาตอน แก้ใข contractNo เพราะใช้เป็น key ในการ update ให้เปลี่ยนไปใช้ ip ของ row นั้นแทน
3. เปลี่ยนการดึง pay ใหม่เป็น ให้ดึงถ้าเทียบกับ paydate แล้วเป็นวันล่าสุดกว่า
4. แก้ flow การ login ให้ login แล้ว check pay 1 ครั้งด้วย